# Backend-Ledger
Backend ledger system built with Node.js, Express, and MongoDB. Includes JWT authentication, bcrypt password hashing, protected routes, email notifications with Nodemailer, account and transaction management, idempotency handling, MongoDB aggregation for balance calculation, and database indexing.
